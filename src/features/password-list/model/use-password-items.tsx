import { usePasswordListQuery } from "@/entities/password-list/queries";
import { useState } from "react";

export function usePasswordItems() {
  const [q, setQ] = useState("");

//   const passwordListQuery = usePasswordListQuery({q: useDebau})
}

